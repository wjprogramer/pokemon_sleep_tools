import 'package:flutter/material.dart';

class PokemonBoxPage extends StatelessWidget {
  const PokemonBoxPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}
